/* Adjusting and automating the gain */

format channels = mono
string file = "monoLisa.wav"
array lisa = read(file, channels)

array automation = line(512, [0, 0.5, 0, 1, 0, 0.5, 0])
lisa = gain(lisa, automation)
play(lisa, channels)